# v0.2.0 发版验收（2026-09-14）

发版内容见 `CHANGELOG.md` 的 `[0.2.0]`：新增 `guard` 并设为默认；生产目录只打开 `claude-code.dispatch_nudge`。

## 发布

- 发版提交 `7e0358b`（`chore: prepare v0.2.0 release`）：两份 `plugin.json` 版本 `0.2.0`，三处描述改为「默认 guard」，
  CHANGELOG 定稿，修正 CLAUDE.md 中「生产目录 `pre_dispatch_apply` 全为 `false`」的过时说法。
- 推送前：`/bin/bash scripts/validate.sh` 通过；`claude plugin validate .` 通过，1 条既有警告（marketplace 缺顶层描述）。
- `git push origin main`：`be1ecb3..7e0358b`；`git push origin v0.2.0`：附注 tag 对象 `df43004`，指向 `7e0358b`，
  `git ls-remote` 核对一致。

## 本机安装更新

| 宿主 | 操作 | 结果 |
|---|---|---|
| Claude Code | `claude plugin marketplace update tier-guard` → `claude plugin update tier-guard@tier-guard` | `0.1.1 → 0.2.0`，`installed_plugins.json` 记录 `gitCommitSha=7e0358b`；需重启会话生效 |
| Codex CLI 0.154.0 | `codex plugin marketplace remove tier-guard` → `codex plugin marketplace add haigeer-labs/tier-guard --ref v0.2.0` → `codex plugin add tier-guard@tier-guard` | `codex plugin list` 显示 `0.2.0`；`config.toml` 中 `ref = "v0.2.0"`；marketplace 检出 `7e0358b`（`describe` = `v0.2.0`） |

- 重装前 `config.toml` 已备份到会话 scratchpad。`[plugins."tier-guard@tier-guard"] enabled = true` 与
  `hooks.state."tier-guard@tier-guard:hooks/codex-hooks.json:pre_tool_use:0:0"` 的 `trusted_hash` 重装后保持不变；
  `hooks/codex-hooks.json` 自 `v0.1.1` 起无改动，推断信任仍有效，**未用真实 Codex 会话复核**。
- 两份安装缓存与 `git archive v0.2.0` 逐文件比对一致；仅有宿主自己生成的文件差异
  （Claude 的 `.in_use`，Codex 的 `.codex-plugin/migrated-command-skills`）。

## 安装后冒烟（直接调用已安装的 hook）

临时 `HOME` 与 `TIER_GUARD_LOG_DIR`，`env -u TIER_GUARD_MODE -u TIER_GUARD_CONFIG`，即只看安装包自带默认值；
不启动宿主会话，不写 `~/.claude` / `~/.codex`。日志 `catalog_identity.sha256` 为
`1ef8289d…d104`，与 `v0.2.0` 中 `config/routing.catalog.v2.json` 的 sha256 一致。

Claude Code（同一会话依次调用）：

| # | 派活 | stdout | 日志 |
|---|---|---|---|
| 1 | `model: "sonnet"` | 空 | `profile=guard pinned=True nudge=none` |
| 2 | 未 pin | `permissionDecision=deny`，无 `updatedInput` | `nudge=denied applied=False` |
| 3 | 未 pin | 仅 `additionalContext` 提醒 | `nudge=reminded applied=False` |
| 4 | `model: "sonnet"` | 空 | `nudge=none` |

- pin 在前不消耗本会话唯一一次 deny；guard 不改写参数。

Codex CLI（未 pin 的 `spawn_agent`）：退出 0、stdout 空，日志 `nudge=none`——`codex-cli.dispatch_nudge=false`，符合预期。

冒烟脚本第一次在 zsh 下运行时 `set -- ${c}` 不分词，三次调用都没带上 `model`，日志 `requested_model` 全为空，
结果作废；改用 `/bin/bash` 重跑得到上表。

## 注意

- 本轮发版所在的 Claude 会话以 `TIER_GUARD_MODE=audit claude` 启动（shell 历史可查），该变量只存在于这个进程，
  shell 配置、`~/.claude/settings.json`、`~/.codex/config.toml`、`launchctl` 中均没有；此类会话仍按 audit 运行。
  新开的会话才会使用默认 guard。
- 公开的两份研究文档含 Claude 会话 UUID 与 Codex 线程 ID（非凭据），推送前已告知用户。

## 安装版真实宿主验收（2026-09-14 01:36–01:37 +0800）

### 方法

- Claude Code CLI `2.1.270`，`claude -p --model sonnet`，工具只放行 `Agent,Skill,Read`。
- 被测对象是已安装的 `tier-guard@tier-guard` `0.2.0`：不加 `--plugin-dir`、不覆盖 `enabledPlugins`，
  并移除 `TIER_GUARD_MODE` / `TIER_GUARD_CONFIG` / `TIER_GUARD_LOG_DIR`，日志写入默认数据目录。
- 同一提示词并行跑两轮：依次派 4 个 `general-purpose` 子代理，**不点名 tier-routing、不指定 A/B/C 的模型**，
  只要求 D 显式传 `model: "sonnet"`。A 只读查重、B 实现 `add(a, b)`、C 生产库 schema 迁移方案取舍、D 只读计数；
  子代理不使用工具。下表只保留描述、指纹与参数，不保存任务原文。
- 判据：父会话 transcript 的 `tool_use` 参数、`decisions.jsonl`、子代理 transcript 中宿主写下的 model。

### 结果

| 轮次 | session | 派活 | 主代理传入 | hook | 实际模型 |
|---|---|---|---|---|---|
| 1 | `786015d4-ffc6-43f5-9191-c891aa0f018b` | A 首次 `toolu_01KMq46nsCCtmsFkxcUQ8peZ` | 未传 | `guard` deny（`nudge=denied`） | 未派出 |
| 1 | 同上 | A 重派 `toolu_01Nv6x1GDLAcx5sf7CAFxrfv` | `haiku` | pin，`nudge=none` | `claude-haiku-4-5-20251001` |
| 1 | 同上 | B `toolu_01DV7fkV26LKtshCLUW5jhkR` | `sonnet` | pin，建议 haiku | `claude-sonnet-5` |
| 1 | 同上 | C `toolu_01GYTjDcFKdnq4tc9FKsnkb9` | `opus` | pin，建议 opus | `claude-opus-5`（报告回读 transcript） |
| 1 | 同上 | D `toolu_01HaVQxwaksrY22XZRh5T7DN` | `sonnet` | pin，`nudge=none` | `claude-sonnet-5` |
| 2 | `0334b787-187a-42d9-aca8-94c8fa11c7e0` | A 首次 `toolu_01BSo4c4p1oZMF7TW1CrcAD5` | 未传 | `guard` deny | 未派出 |
| 2 | 同上 | A 重派 `toolu_01WFXLZtLkqhKRzaBAeJ5JMD` | `haiku` | pin | `claude-haiku-4-5-20251001` |
| 2 | 同上 | B `toolu_017e7HuM5oM5kTpKkbJh1SS8` | `sonnet` | pin，建议 opus | `claude-sonnet-5` |
| 2 | 同上 | C `toolu_01SFWhynU45LVwvBE4FavUS9` | `opus` | pin，建议 opus | `claude-opus-5`（报告回读 transcript） |
| 2 | 同上 | D `toolu_011fG8rNwTtUJKBpXjsG26L6` | `sonnet` | pin，`nudge=none` | `claude-sonnet-5` |

- 两轮都没有调用 `Skill`；所有记录 `profile=guard`、`applied=false`、catalog sha256 前缀 `1ef8289d9b08`（与 tag 一致）。
- 第一次未 pin 派活被拦截后，主代理本会话剩余 4 次派活全部显式传参，且档位与任务类别一致（只读 haiku、实现 sonnet、
  取舍 opus）；取舍类 0 次降档；pin 的 D 0 次被提醒或拦截。
- 每轮 4 条 SubagentStop 全部入账；C 两轮 hook 时刻都未取到 model，报告回读 transcript 后补齐。
- 已安装 `/tier-report`：当前 mode **guard**（来源：路由配置默认值）；本轮新增「拦截 2 次 / 未触发 8 次」，
  「提醒或拦截之后同会话派活 8 次，其中显式传参 8/8」，「pin 的派活被提醒或拦截 0 次」。
- 费用（`total_cost_usd`）：第 1 轮 0.4669，第 2 轮 0.3923。`modelUsage` 两轮都含
  `claude-haiku-4-5-20251001`、`claude-sonnet-5`、`claude-opus-5[1m]`；`[1m]` 后缀的来源未核实。
- B 的 hook 字面建议两轮不同（haiku / opus），源于两轮主代理写的任务文本不同（指纹不同）；pin 语义下不影响执行。

### 结论

- 待补宿主验收中的「Claude Code CLI v2 完整端到端验收（A/B/C/D）」与「自然触发」在安装版 `0.2.0` 上通过（Claude）。
- Codex 闸门仍关闭，未重跑；Codex 自然使用以 Task 15（主代理 12/12 自行显式传参）为准。

### 观察（未修复）

- `/tier-report` 的「建议档 vs 实际执行档（SubagentStop）」一节只按 v1 字段（`decision.tier`、`actual_tier`）关联，
  对 v2 记录恒为「已关联 0 / N」，与同一报告表格里已显示的实际执行（本轮 8/8）矛盾，容易误读为没有观测到实际执行。

## v0.2.1 补丁发版（2026-09-14）

内容：修复上节「观察」——`/tier-report`「建议档 vs 实际执行档」对 v2 记录改报「v2：已观测实际执行 X / N 次」
（按 `tool_use_id` 关联、同样回读未落盘 transcript），v1 分母只数旧记录；报告不比较实际档高低。
只改报告脚本、回归与变异条目，hook 判据与 `hooks/codex-hooks.json` 未改动。

- 修复提交 `7a3630e`：`hooks/test-tier-commands.sh` 新增 3 条先红后绿、1 条 v1 口径保持；命令回归 49/0；
  `scripts/mutation-check.py --only report` 符合预期 14/14（新增 2 条均被抓到）。
- 发版提交 `cd8cf67`：两份 `plugin.json` 版本 `0.2.1`，CHANGELOG 定稿。推送前 `validate.sh` 通过，
  `claude plugin validate .` 通过（同一条既有警告）。
- `git push origin main`：`c7ed82f..cd8cf67`；`git push origin v0.2.1`：附注 tag 对象 `2449d28`，指向 `cd8cf67`，
  `git ls-remote` 核对一致。

安装更新：

| 宿主 | 结果 |
|---|---|
| Claude Code | `claude plugin update` 报告 `0.2.0 → 0.2.1`，`installed_plugins.json` 记录 `gitCommitSha=cd8cf67`；需重启会话生效 |
| Codex CLI | marketplace 删除后以 `--ref v0.2.1` 重新添加并重装；`codex plugin list` 为 `0.2.1`，检出 `describe` = `v0.2.1`；`enabled = true` 与 hook `trusted_hash` 未变（未用真实 Codex 会话复核） |

- 两份安装缓存与 `git archive v0.2.1` 逐文件一致，仅宿主生成文件差异（同 0.2.0）。重装前 `config.toml` 已备份到会话 scratchpad。
- 已安装 `0.2.1` 的 `/tier-report` 读本机默认数据目录：当前 mode **guard**；该节显示
  「v2：已观测实际执行 8 / 30 次」，不再出现「已关联 0 / N」。8 条为上节安装版验收入账的实际执行，
  其余 22 条是 `0.1.1` 时期未记录 SubagentStop 的旧 audit 派活。

## v0.2.1 真实 Codex 会话复核（2026-09-14 10:51–10:53 +0800）

目的：核实重装后 Codex 对已安装 `0.2.1` hook 的信任仍然有效（此前只凭 `trusted_hash` 未变与 `hooks/codex-hooks.json` 未改动推断）。

- Codex CLI `0.154.0` 交互式 TUI，tmux 真实 PTY；父模型 `gpt-5.6-terra / high`，`-s read-only -a never`。
  不停用插件，移除 `TIER_GUARD_MODE` / `TIER_GUARD_CONFIG` / `TIER_GUARD_LOG_DIR`，日志写默认数据目录。
- 提示词只要求创建 1 个不使用工具、回复固定文本的原生子代理；不点名 tier-routing，不提模型或档位。
- 新建的隔离工作目录触发了 Codex 的目录信任提示，选择「Yes」前备份 `config.toml`；之后未出现任何 hook 审核提示。

| 证据 | 结果 |
|---|---|
| 默认数据目录 `decisions.jsonl` | 基线 14 行 → 15 行；新增 `codex-spawn`，`routing_version=2`、`profile=guard`、`host=codex-cli`、`task_visibility=opaque_token` |
| 请求参数 | `gpt-5.6-luna / medium`，`pinned=True`；`nudge=none`（`codex-cli.dispatch_nudge=false`），`applied=False`，无 fallback |
| catalog sha256 | 前缀 `1ef8289d9b08`，与 tag 一致 |
| Codex 线程库 | 父线程 `01a09dd4-7090-7d72-bfea-442f539d9ad5` 为 `gpt-5.6-terra / high`；子线程 `01a09dd4-aec5-7d32-bca5-13b7a13a9bd3` 为 `gpt-5.6-luna / medium` |
| TUI | 主代理先自行读取 `tier-guard:tier-routing` skill，再派出子代理；最终回复为固定文本 |

- 结论：重装 `0.2.1` 后 Codex 的插件 hook 信任有效，hook 在真实派活上运行并记录。
- 顺带观察：本轮主代理在未被点名时自行读取了 tier-routing 并按目录显式传参，与 Task 15 中 Codex 12/12 自行显式传参一致；单次样本，不改变结论。
- 配置变更：`config.toml` 相比选择信任前的备份只多了该隔离工作目录的 `trust_level = "trusted"`，
  `hooks.state` 与 `[marketplaces.tier-guard]` 未变（与此前各轮 Codex 实测留下的目录信任条目同类，未删除）。
- 等待脚本以「终端出现 3 次固定文本」为完成条件，实际只显示 2 次，因而报了超时；证据以日志与线程库为准。
